type TConfig = {
  SECRET_KEY: string,
  DB_URL: string,
  PORT: number,
  DB_PASSWORD: string
}

const config: TConfig = {
  SECRET_KEY: 'mysecret123',
  DB_URL: 'url://nonefornow/xhs1fDmx93oCmM5',
  PORT: 4000,
  DB_PASSWORD: ''
};

export default config;
